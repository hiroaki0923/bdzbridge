.DS_Store
.pytest_cache/
.ruff_cache/
__pycache__/
notes/
CLAUDE.local.md
server/tests/fixtures/*.dat

# signing material and store credentials: never in a public repository (see docs/porting.md)
*.p12
*.cer
*.mobileprovision
AuthKey_*.p8
*.jks
*.keystore
key.properties

# Swift / Xcode build products. The Xcode project and the Info.plist are generated by XcodeGen from
# app/project.yml but committed anyway, because Xcode Cloud looks for a project in the repository before it
# runs anything of ours; only the per-user state inside is ignored.
app/BDBridge.xcodeproj/xcuserdata/
app/BDBridge.xcodeproj/project.xcworkspace/xcuserdata/
app/Signing.local.xcconfig
app/TestFlight.local.env
app/build/
.build/
.swiftpm/
DerivedData/
*.xcuserdatad
